{# about.jinja – 소개 페이지 #}
{% extends "layout" %}
{% block title %}{{ super() }} | {{ title }} {% endblock %}
{% block body %}
<h1>{{ title }}</h1>
{# {{ about_text }}로 설명 텍스트 삽입 #}
{# "Simple demonstration layout..."이 주입됨 #}
<p>{{ about_text }}</p>
{% endblock %}
